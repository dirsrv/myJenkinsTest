node {
    stage('Create file'){
        sh 'touch NewFile' 
    }
    
    stage('Write to file'){
        sh 'echo "This is simple tets" > NewFile'
    }
        
    stage('Print file'){
        sh 'cat NewFile'
    }    
}
